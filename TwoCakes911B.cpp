#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,a,b,ans=0,max=-100000;
    cin>>n>>a>>b;
    if (a+b==n)
    	ans=1;
    else
    {
    	for (int i=1;i<n;i++)
    	{
    		int comp=min(a/n)
    	}
    }
    cout<<ans<<endl;
    return 0;
}